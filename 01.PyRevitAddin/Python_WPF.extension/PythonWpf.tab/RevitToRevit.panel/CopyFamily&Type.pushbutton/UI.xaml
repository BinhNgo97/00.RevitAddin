<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copy Family/Element Types From RVT" Height="600" Width="560"
        WindowStartupLocation="CenterScreen" Background="#DDD">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="12"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Row 0: Select File -->
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Select File Revit :" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button x:Name="btnFilepath" Grid.Column="1" Height="34" Margin="8,0,0,0"
              Content="Filepath" HorizontalAlignment="Stretch" Background="#B3B3B3"/>
    </Grid>

    <!-- Row 2: Select Category -->
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Select Category :" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
      <ComboBox x:Name="cmbCategory" Grid.Column="1" Height="34" Margin="8,0,0,0"
                Background="#B3B3B3" />
    </Grid>

    <!-- Row 4: List of types with checkbox -->
    <StackPanel Grid.Row="4">
      <TextBlock Text="Select ElementType to Tranfer :" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
      <Border BorderBrush="#AAA" BorderThickness="1" Background="#C8C8C8">
        <ListView x:Name="lstTypes" SelectionMode="Extended" Background="#C8C8C8">
          <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            </Style>
          </ListView.ItemContainerStyle>
          <ListView.ItemTemplate>
            <DataTemplate>
              <DockPanel LastChildFill="True" Margin="4">
                <!-- Checkbox bound to selection state -->
                <CheckBox Margin="4,0" VerticalAlignment="Center"
                          IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}"/>
                <TextBlock Text="{Binding Name}" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
              </DockPanel>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Border>
    </StackPanel>

    <!-- Row 6: Transfer -->
    <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="btnTransfer" Content="Tranfer" Width="110" Height="34" Background="#B3B3B3"/>
    </StackPanel>
  </Grid>
</Window>