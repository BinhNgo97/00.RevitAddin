<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Void from Link"
        Width="350"
        Height="400"
        WindowStartupLocation="CenterScreen"
        Background="#F2F2F2"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="Create Void From Link"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#2B2B2B"
                   Margin="0,0,0,15"/>

        <!-- SELECT BUTTON -->
        <Button x:Name="btnSelectElements"
                Grid.Row="1"
                Height="40"
                Margin="0,0,0,15"
                Background="#E0E0E0"
                BorderBrush="#A0A0A0"
                BorderThickness="1"
                FontSize="12"
                FontWeight="SemiBold"
                Content="Select Link Element"
                Cursor="Hand"/>

        <!-- FAMILY NAME -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#C0C0C0"
                BorderThickness="1"
                Padding="10"
                Margin="0,0,0,15">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Family Name"
                           VerticalAlignment="Center"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="#333333"/>

                <TextBox x:Name="txtFamilyName"
                         Grid.Column="1"
                         Height="26"
                         Padding="6,3"
                         FontSize="13"
                         Text="{Binding FamilyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- CREATE BUTTON -->
            <Button x:Name="btnCreate"
                    Grid.Row="0"
                    Height="45"
                    Margin="0,0,0,15"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="Bold"
                    Content="Create Void"
                    Cursor="Hand"/>

            <!-- STATUS / RESULT -->
            <Border Grid.Row="1"
                    Background="White"
                    BorderBrush="#C0C0C0"
                    BorderThickness="1"
                    Padding="12">

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="txtResult"
                               Text="{Binding StatusMessage}"
                               FontSize="13"
                               Foreground="#2B2B2B"
                               TextWrapping="Wrap"/>
                </ScrollViewer>

            </Border>
        </Grid>
    </Grid>
</Window>
